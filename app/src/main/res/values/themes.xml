<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Pre-Compose window theme. DayNight so the launch window does not flash white on a dark device. -->
    <style name="Theme.DayOwl" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
